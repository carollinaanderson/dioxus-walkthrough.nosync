services:
  postgres:
    image: postgres:16
    container_name: myapp_ch05_postgres
    environment:
      POSTGRES_USER: myapp
      POSTGRES_PASSWORD: myapp
      POSTGRES_DB: myapp_ch05
    ports:
      - "5435:5432"
    volumes:
      - pgdata_ch05:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U myapp"]
      interval: 3s
      timeout: 3s
      retries: 10
volumes:
  pgdata_ch05:
